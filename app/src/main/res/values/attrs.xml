<resources>
    <declare-styleable name="Schedule">
        <!-- 横线的一些信息 -->
        <attr name="lineColor" format="color" />
        <attr name="lineWidth" format="dimension" />
        <attr name="lineTextColor" format="color" />
        <attr name="lineTextSize" format="dimension" />
        <!-- +号的颜色 -->
        <attr name="addIconColor" format="color" />
        <attr name="itemColor" format="color" />
        <!-- 圆角-->
        <attr name="circleCorner" format="dimension" />
    </declare-styleable>

    <declare-styleable name="ScrollSchedule">
        <!-- 横线的一些信息 -->
        <attr name="itemOffest" format="dimension" />
    </declare-styleable>
</resources>